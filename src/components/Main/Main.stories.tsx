import { Meta, Story } from '@storybook/react'
import React from 'react'

import Main from '.'

export default {
  title: 'Main',
  component: Main
} as Meta

export const Basic: Story = () => <Main />
